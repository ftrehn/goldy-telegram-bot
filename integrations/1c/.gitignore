# Учётка локальной базы (путь, пользователь, пароль) и значения констант,
# среди которых токен приёмника: личные файлы, в репозиторий не попадают.
# Образцы рядом: conn.txt.example, settings.json.example.
conn.txt
settings.json

# Всё, что порождают скрипты: журнал конфигуратора, отчёты прогонов,
# пачки JSON из dump_batches.ps1, собранный .cfe.
out/
*.bak
