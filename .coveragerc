[run]
parallel = True
data_file = .coverage
source = src
omit =
    tests/*
    src/goldy/_version.py
    ; Entry points: assembly only, and nothing to assert without standing the
    ; process up. What they build is covered through the integration container.
    ; Must match sonar.coverage.exclusions, or Sonar reports them as uncovered.
    src/goldy/telegram_bot.py
    src/goldy/worker_app.py
    src/goldy/scheduler_app.py
    src/goldy/infrastructure/persistence/migrations/*

[report]
show_missing = True
skip_covered = True
exclude_lines =
    pragma: no cover
    pragma: no coverage
    if TYPE_CHECKING:
    ^\s*\.\.\.$
    @(abc\.)?abstractmethod
